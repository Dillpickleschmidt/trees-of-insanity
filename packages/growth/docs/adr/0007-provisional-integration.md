---
status: accepted
---

# Keep plant integration choices explicit and provisional

Use deterministic fixed-step redevelopment from age zero, normalized sphere overlap, a root-vigor-relative shedding threshold, deepest-terminal continuation, equal terminal split, and the documented age scale. `docs/model.md` defines values and rationale. These are tuning decisions, not paper facts.
