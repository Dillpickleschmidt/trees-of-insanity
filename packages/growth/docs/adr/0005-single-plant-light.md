---
status: accepted
---

# Use intra-plant self-collision for single-plant light

Module light exposure is `Q(u) = exp(-f_collisions(u))`, using intersections among the same plant's module bounding volumes. Inter-plant shadow propagation is outside this package's current scope.
