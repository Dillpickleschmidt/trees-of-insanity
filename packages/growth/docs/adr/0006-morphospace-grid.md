---
status: accepted
---

# Use an even 3x3 morphospace grid

Nine prototypes occupy levels `{1/6, 1/2, 5/6}` with determinacy increasing across columns and apical control across rows. Selection chooses the nearest seed to `(apical control, vigor-scaled determinacy)`.
