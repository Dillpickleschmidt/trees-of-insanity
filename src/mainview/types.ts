// View-facing aliases for the native command shapes (single source of truth is
// src/shared/appCommands.ts, which mirrors the C++ application-command seam).

export type {
	AppState,
	GrowthSnapshotSummary,
	PlantTypeParameterDescriptor as ParameterDescriptor,
	PlantTypeSummary,
	PrototypeSummary,
	PrototypeTree,
	PrototypeTreeItem,
	WorkspacePreview,
} from "../shared/appCommands";
