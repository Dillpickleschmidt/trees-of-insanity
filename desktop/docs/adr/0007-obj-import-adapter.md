# ADR 0007: OBJ is a desktop import adapter

Status: accepted

Prototype OBJ parsing belongs to desktop model import. Convert imported geometry immediately into growth public types. Growth never knows paths, OBJ syntax, or bundled assets.
