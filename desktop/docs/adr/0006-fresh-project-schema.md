# ADR 0006: Fresh local Project schema

Status: accepted

Persist one local JSON Project using the current unreleased schema. Validate on load and save atomically. Do not add migration or legacy-schema behavior before release.
