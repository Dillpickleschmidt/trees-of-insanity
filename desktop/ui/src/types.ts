// View-facing aliases for the native command shapes (single source of truth is
// src/shared/desktopActions.ts, which mirrors the C++ application-command seam).

export type {
	AppState,
	GrowthSnapshotSummary,
	HdriEnvironment,
	PlantDiagnostics,
	PlantState,
	PlantTypeSummary,
	PrototypeSummary,
	PrototypeTree,
	PrototypeTreeItem,
	ViewportPreferences,
	ViewportPreferencesView,
	Workspace,
	WorkspacePreview,
} from "./shared/desktopActions";
